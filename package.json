{
  "name": "mi_mia",
  "version": "2.7.0",
  "description": "mi_mia v2 GAS project",
  "scripts": {
    "lint": "eslint --ext .gs src/",
    "test": "jest",
    "push": "clasp push",
    "push:dist": "cp .clasp.json .clasp.json.bak && cp .clasp.dist.json .clasp.json && clasp push -f; cp .clasp.json.bak .clasp.json && rm .clasp.json.bak",
    "push:all": "npm run push && npm run push:dist",
    "pull": "clasp pull",
    "watch": "clasp push --watch"
  },
  "devDependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^1.0.83",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.5"
  }
}
